services:
  sshbridge-dev:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: sshbridge-dev
    restart: unless-stopped
    ports:
      - "8081:8080"
    volumes:
      - sshbridge-dev-data:/app/data
    environment:
      PORT: "8080"
      NODE_ENV: development
    depends_on:
      - guacd-dev
    networks:
      - sshbridge-dev-net

  guacd-dev:
    image: guacamole/guacd:1.6.0
    container_name: guacd-dev
    restart: unless-stopped
    networks:
      - sshbridge-dev-net

volumes:
  sshbridge-dev-data:
    driver: local

networks:
  sshbridge-dev-net:
    driver: bridge
